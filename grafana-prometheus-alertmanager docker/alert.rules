groups:
- name: node-exporter
  rules:
  - alert: InstanceDown
    expr: up{job="node"} == 0
    for: 10s
    labels:
      severity: page
    annotations:
      summary: "Instance {{ .instance }} down"
      description: "{{ .instance }} of job {{ .job }} has been down for more than 5 minutes."
